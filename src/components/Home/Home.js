import React from 'react';
import Banner from '../Banner/Banner';
import Login from '../Login/Login';

const Home = () => {
    return (
        <div>
            <Banner/>
            <Login/>
        </div>
    );
};

export default Home;